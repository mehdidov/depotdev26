pipeline {
    agent any
    stages {
        stage('Nettoyage') {
            steps {
                cleanWs()
            }
        }
        stage('Clonage du Depot') {
            steps {
                sh 'git clone https://github.com/mehdidov/depotdev26.git'
            }
        }
    }
}